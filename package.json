{
  "name": "career-pilot-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "npm run build --prefix frontend",
    "install": "npm install --prefix frontend",
    "build:portfolio": "npm run build:portfolio --prefix frontend"
  },
  "dependencies": {
    "bullmq": "^5.78.0",
    "ioredis": "^5.11.1",
    "node-fetch": "^3.3.2"
  }
}
